import { DataFormatDirective } from './data-format.directive';

describe('DataFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new DataFormatDirective();
    expect(directive).toBeTruthy();
  });
});
